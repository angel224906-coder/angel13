# You can supply either a full URI or user/password+host.
# Example full URI:
# MONGODB_URI=mongodb://user:password@host:27017/book_inventory

# Or provide user/password + host below and the server will build the URI.
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_HOST=127.0.0.1:27017
PORT=3000
